# Сервис эксплуатационной аналитики обмена с ЕГИСЗ

Эксплуатационная аналитика проксирующего шлюза между МИС клиник и РЭМД: журнал обмена → документная витрина в PostgreSQL → четыре дашборда Metabase.

**Для кого:** дежурная поддержка, аналитики, руководство, клиенты-клиники.

---

## Содержание

- [Контекст: ЕГИСЗ, СЭМД, обмен и где в нём наш контур](#контекст-егисз-сэмд-обмен-и-где-в-нём-наш-контур)
- [Состав аналитики](#состав-аналитики)
- [Архитектура и поток данных](#архитектура-и-поток-данных)
- [Источник данных](#источник-данных)
- [ELT-конвейер](#elt-конвейер)
- [Парсинг и нормализация](#парсинг-и-нормализация)
- [Сборка документа](#сборка-документа)
- [Версии и идентичность документа](#версии-и-идентичность-документа)
- [Классификация ошибок](#классификация-ошибок)
- [DWH-модель](#dwh-модель)
- [Дашборды Metabase](#дашборды-metabase)
- [Запуск контуров](#запуск-контуров)
- [Глоссарий](#глоссарий)
- [Приложение: типы ошибок](#приложение-типы-ошибок)
  - [Справочник активных правил классификации](#справочник-активных-правил-классификации)

---

## Контекст: ЕГИСЗ, СЭМД, обмен и где в нём наш контур

**ЕГИСЗ** — единая государственная информационная система в сфере здравоохранения. Для клиники ключевое взаимодействие — передача **СЭМД** (структурированных электронных медицинских документов) в **РЭМД** (реестр электронных медицинских документов).

Обмен асинхронный: МИС отправляет CDA в SOAP-конверте, получает синхронное «принято к обработке», затем — callback с результатом (`emdrId` или список ошибок). Одна отправка СЭМД — **как минимум два сообщения** в журнале: исходящее и callback.

Между МИС и ЕГИСЗ стоит проксирующий шлюз. Он пишет все сообщения в журнальную БД **`proxy_egisz`** (Firebird). Сервис строит на её основе документную витрину для BI и оперативного контроля.

---

## Состав аналитики

Пять срезов эксплуатационной аналитики; каждый опирается на витрины DWH и вкладки дашборда «Интеграция с ЕГИСЗ» (или отдельные клиентские дашборды).

| Срез | Содержание | Витрина / представление |
| ---- | ---------- | ----------------------- |
| Отправка и очередь | Объёмы, статусы, динамика по клиникам и типам СЭМД | `rpt_documents` |
| Обратная связь | Документы без callback, корреляция исходящего сообщения и ответа | `rpt_documents_waiting`, `rpt_documents` |
| Ошибки | Канонический `error_type`, разбивка по категориям | `rpt_error_breakdown`, `rpt_network_errors` |
| Полнота | Атрибуты документа, `emdrId`, расхождения JID | `rpt_documents`, `document_attributes` |
| Версии | Текущая версия vs полный аудит попыток, health группировки | `rpt_documents`, `rpt_document_versions`, `rpt_health_versions` |
| Недельная динамика | SLI по неделям, контрольная p-карта, структура ошибок по категориям | `rpt_documents_weekly`, `rpt_error_breakdown_weekly` |
| Контур загрузки | Курсор журнала, хвост raw, health-сигналы | `rpt_health_*`, `elt_state` |

---

## Архитектура и поток данных

Схема **ELT**: Airflow извлекает и загружает; парсинг, нормализация, сборка документа и классификация ошибок — в PostgreSQL (`db/parts/`). Metabase читает представления `rpt_*`.

```
proxy_egisz (EXCHANGELOG, JPERSONS, EGISZ_LICENSES)
        │  extract / reconcile
        ▼
exchangelog_raw  ──transform──►  transactions  ──►  documents
        │                              │                │
        │                              └──── document_attributes
        ▼
   elt_state.last_logid          dim_* (справочники)
                                        │
                                        ▼
                                   rpt_*  ──►  Metabase
```

| Компонент | Роль |
| --------- | ---- |
| `proxy_egisz` (Firebird) | Журнал обмена и справочники — источник |
| Airflow | Три DAG: документы, справочники, сверка |
| `dwh_egisz` (PostgreSQL) | Raw, факты, справочники, правила ошибок, `rpt_*` |
| Metabase | Четыре дашборда, четыре модели поверх `rpt_*` |

Служебные БД Airflow и Metabase отделены от `dwh_egisz`.

---

## Источник данных

Инкрементальная выборка журнала — по монотонному `EXCHANGELOG.LOGID` (`LOGID > last_logid`).

| Таблица | Grain | Поля |
| ------- | ----- | ---- |
| `EXCHANGELOG` | 1 строка = 1 SOAP-сообщение | `LOGID`, `LOGDATE`, `CREATEDATE`, `MSGID`, `LOGSTATE`, `LOGTEXT`, `MSGTEXT` |
| `JPERSONS` | Организация | `JID` (`bigint`), `JNAME`, `JINN`, `FIR_OID` |
| `EGISZ_LICENSES` | Лицензия МО | `mo_uid`, `JID` |

`MSGTEXT` — полный SOAP/XML (BLOB в источнике, `text` в DWH). Бизнес-поля извлекаются при transform; исходный журнал не меняется. `LOGDATE` — служебная метка строки журнала; дата документа — из payload (`creationDateTime`).

---

## ELT-конвейер

Три независимых DAG; у каждого `max_active_runs = 1`.

### Основной поток документов

```
extract_exchangelog ▸ transform_exchangelog ▸ refresh_report_marts
```

| Задача | Действие |
| ------ | -------- |
| `extract_exchangelog` | Читает `elt_state.last_logid`. Пока в `exchangelog_raw` есть строки с `logid` выше watermark — источник не читается. Иначе fetch `EXCHANGELOG` → UPSERT в `exchangelog_raw`, `ANALYZE`. |
| `transform_exchangelog` | Цикл: `transform_raw_to_facts(from, to)` — парсинг raw в `transactions`, сборка `documents`, сдвиг watermark через `GREATEST`. |
| `refresh_report_marts` | При `transformed > 0` — `REFRESH MATERIALIZED VIEW CONCURRENTLY` витрин динамики (`rpt_documents_weekly` / `rpt_error_breakdown_weekly` / `rpt_documents_monthly` / `rpt_error_breakdown_monthly`). |

При сбое watermark не откатывается: следующий прогон перечитывает тот же диапазон. Дублей документов нет (UPSERT по `dwh_id`).

### Справочники

| Задача | Действие |
| ------ | -------- |
| `sync_dimensions` | UPSERT `dim_organizations` ← `JPERSONS`, `dim_licenses` ← `EGISZ_LICENSES`. При изменениях — `reconcile_document_attributes_ui()` (имена клиник, JID в `document_attributes`). |

### Сверка полноты журнала

| Задача | Действие |
| ------ | -------- |
| `reconcile_proxy_raw` | Set-diff `LOGID` источника и `exchangelog_raw` за окно lookback (дефолт 30 дней); догрузка missing → transform с prefix-lookback. Watermark не меняет. Skip при raw-хвосте extract. |

Callback'и и backfill шлюза могут появиться с `LOGID` ниже watermark. Основной поток их не видит (`LOGID > last_logid`); сверка закрывает пропуски.

### Периодичность

| Поток | Интервал |
| ----- | -------- |
| Документы | каждые 5 мин |
| Справочники | каждый час |
| Сверка | раз в сутки |

---

## Парсинг и нормализация

Каждая строка `exchangelog_raw` парсится **один раз** функцией `parse_exchangelog_row(msgtext, msgid, logtext)`; результат пишется в `transactions` (`xml_*`). Попытка парсинга фиксируется в узкой таблице `exchangelog_parse_attempts` (LOGID): строка, чей payload не даёт реквизитов (нет `msgid`/`localUid`/`emdrId`/`getDocumentFile`), в `transactions` не попадает, но повторно не парсится — без маркера полножурнальный lookback reconcile перепарсивал такой «мусор» каждым окном. Дальнейшая сборка документа читает только `transactions`.

Утилиты (`db/parts/20_functions_parsing.sql`):

| Функция | Назначение |
| ------- | ---------- |
| `xml_text` | Текст тега XML без привязки к namespace |
| `normalize_message_id` | `urn:uuid:` → единый формат |
| `dwh_id` | Ключ **экземпляра/версии** документа: `lower(localUid)` (см. §«Версии и идентичность документа») |
| `resolve_document_jid` | JID: `mo_uid` из XML → fallback по host/gost-endpoint |
| `normalize_semd_code` | Код СЭМД из payload |
| `classify_async_status` | Статус сообщения до уровня документа |

Извлекаемые поля:

| Поле | Источник | Правило |
| ---- | -------- | ------- |
| MSGID запроса | `<messageId>` / `MSGID` | ЕГИСЗ `request_id`; на документе — `request_msgid` (MSGID отправки) |
| Связанное сообщение | `<relatesToMessage>` / `<relatesTo>` | ЕГИСЗ `response_to_request_id`; на документе — `relates_to_msgid` (= `request_msgid` у склеенных) |
| Локальный ID | `<localUid>` / `<DOCUMENTID>` | Приоритет `localUid` |
| ID в РЭМД | `<emdrId>` | |
| OID организации | `<organization>` | → `dim_licenses.mo_uid` → JID |
| Код / название СЭМД | `<kind>`, `<name>` | Название также из `dim_semd_types` (НСИ `1.2.643.5.1.13.13.11.1520`) |
| Ошибка | `<code>`, `<message>`; при `LOGSTATE = 3` — `LOGTEXT` | Вход классификатора |
| Дата создания | `<creationDateTime>` | `safe_cast_timestamptz` |
| Номер протокола | `<documentNumber>` / `PROTOCOLID` | → `doc_number`; ключ группировки версий (см. §«Версии и идентичность документа») |

**Статус сообщения** (`classify_async_status`) — проекция одной строки журнала: `success`, `error`, `pending`, `unknown`. Синхронный `RegisterDocumentResponse` с `<status>success</status>` означает только приём запроса (`pending`), не регистрацию. Регистрация — асинхронный callback с `registerDocumentResult` / `<documentStatus>Зарегистрировано</documentStatus>`. `LOGSTATE = 3` → `error` (транспорт).

**Статус документа** в `documents` — итог по цепочке сообщений одной отправки (4 канонических, `dim_document_status`):

| `status` | Метка | LOGID-якорь | MSGID-якорь |
| --- | --- | --- | --- |
| `waiting` | **Отправлено** (серый): после отправки (`getDocumentFile`) до финального callback РЭМД | `request_logid` | `request_msgid` |
| `network_error` | Ошибка связи | `result_logid` (LOGSTATE 3) | — |
| `async_error` | Ошибка проверки РЭМД | `result_logid` | `relates_to_msgid` |
| `success` | Успешно зарегистрирован | `result_logid` | `relates_to_msgid` |

Транспорт СЭМД именуется по записям журнала: **отправка** (`getDocumentFile`) = `request_*`, **исход** (ответ РЭМД или сбой связи) = `result_*`. В витрине `logid = COALESCE(result_logid, request_logid)` («Отправлено» несёт LOGID отправки).

**JID:** `resolve_document_jid(org_oid, endpoint)`. Флаг `clinic_jid_mismatch` — расхождение OID из XML, `JPERSONS.fir_oid`, `EGISZ_LICENSES.mo_uid`.

---

## Сборка документа

Два слоя:

1. **Сообщение** — `transform_raw_to_facts` парсит новые строки `exchangelog_raw` в `transactions` (поля `xml_*`, классификация callback на уровне строки). Повторный парсинг той же строки пропускается по маркеру `exchangelog_parse_attempts` (пишется на весь просканированный диапазон, включая строки, не прошедшие фильтр вставки). Для строк `getDocumentFile` при парсинге фиксируется `jid` (`resolve_document_jid` вызывается один раз на строку); связывание позднего callback с запросом (`gdf_ref`) идёт по `transactions.jid` через индекс `idx_transactions_gdf_jid_logid`, а не регексом по payload на чтении — иначе полножурнальный lookback reconcile вырождается в O(батч × журнал × regex).

2. **Экземпляр документа** — UPSERT в `documents` (PK `dwh_id` = `lower(localUid)`). Несколько строк журнала одной отправки сходятся в одну запись: callback привязывается по `relatesTo` / `emdrId` (`normalize_message_id`). Накапливаются `first_sent_at`, `last_callback_at`, `registered_at`, итоговый `status`, `error_types`, `jid`. Грейн записи — **версия отправки**, а не «вечный» документ: правка СЭМД меняет `localUid` ⇒ новый экземпляр (см. §«Версии и идентичность документа»).

3. **Группы версий** — `recompute_document_versions(dwh_ids)` пересчитывает `document_group_id`, цепочку `supersedes_*` и `is_current_version` для затронутого батча; вызывается в конце `transform_raw_to_facts` и при полной инициализации DWH.

Lookback при transform: extract использует окно по ширине батча; reconcile передаёт prefix журнала, чтобы поздний callback связался с ранним `getDocumentFile`.

`document_attributes` (1:1 к экземпляру): lineage OID, host, endpoint, `request_msgid` (MSGID отправки — на грейне документа `result_msgid` хранит MSGID ответа, поэтому MSGID запроса берётся из source-транзакции), `contour` (контур обмена, зафиксированный на грейне документа — последний непустой `transactions.contour` по строкам документа, чтобы отчётный слой не читал message-грейн), BI-маски; обновляется после transform и `sync_dimensions`.

### Учёт отправленных vs корпус результатов

«Отправлено» (`status='waiting'`) — отправка без финального вердикта РЭМД. Учёт раздельный, грейн — `is_current_version`:

| Срез | Условие | Где используется |
| ---- | ------- | ---------------- |
| **Отправлено** | `status = 'waiting'` | Счётчик объёма/очереди; возрастные сегменты `rpt_documents_waiting.wait_segment`; вкладка «Отправленные» |
| **Корпус результатов** | `status <> 'waiting'` | KPI, доли успеха/ошибок, время доставки, здоровье клиник, error-топы |

Доля успеха = `success / (success + async_error + network_error)`. Отдельного флага «финальный» нет — корпус определяется полем `status`.

---

## Версии и идентичность документа

Журнал хранит SOAP-обёртку РЭМД; тело CDA (base64) шлюзом не сохраняется. Парсер извлекает из обёртки `localUid`, `documentNumber`, `emdrId`, корреляционные MSGID. Поля CDA `setId` / `versionNumber` / `relatedDocument` в `MSGTEXT` не парсятся — в контракте журнала их нет.

| Уровень | Смысл | Источник в обёртке | Поле DWH |
| ------- | ----- | ------------------ | -------- |
| Экземпляр / версия | UUID конкретной выгрузки СЭМД; меняется при правке и ре-выгрузке | `localUid` | `dwh_id` (PK) |
| Логический документ | Цепочка версий одного документа в МИС | `jid` + `semd_code` + `doc_number` | `document_group_id` |

`doc_number` — номер протокола/ИБ (`PROTOCOLID` / тег `documentNumber`), подтягивается из `transactions` в `documents` на шаге группировки. МИС передаёт `documentNumber` не во всех отправках — экземпляры без номера остаются одиночными группами (`singleton`).

Логика `recompute_document_versions` (`db/parts/50_transform.sql`):

| Условие | `document_group_id` | `document_group_confidence` | Группа версий |
| ------- | ------------------- | --------------------------- | ------------- |
| Нет `jid`, `semd_code` или `doc_number` | `dwh_id` | `singleton` | нет (экземпляр сам себе группа) |
| Ключ заполнен, в группе 1 экземпляр | `dwh_id` | `singleton` | нет |
| Ключ заполнен, 2…50 экземпляров (`c_cap`) | `grp_key` из `recompute_document_versions` | `doc_number` | да |
| Ключ заполнен, > 50 экземпляров | `dwh_id` | `singleton` | нет (защита от переиспользования счётчика протокола) |

В реальной группе: `semd_version_number` — порядок по `first_sent_at`; `is_current_version = true` ровно у одного экземпляра (приоритет `success`, иначе последнее IPS-событие); связь версий — `supersedes_dwh_id` / `superseded_by_dwh_id`.

Витрины: `rpt_documents` — только `is_current_version`; полный аудит — `rpt_document_versions`; контроль группировки — `rpt_health_versions` (размер групп, коллизии `localUid` по `transactions`).

---

## Классификация ошибок

РЭМД отдаёт ошибки как SOAP-faultcode, массив `<item>` (`code` + `message`), фрагменты Schematron; контур ИЭМК (IHE XDS.b) — атрибутами `<rs:RegistryError errorCode codeContext>`, которые `xml_registry_errors` приводит к тем же парам `code`/`message`. Правила — `dim_error_rules` (`db/parts/30_error_rules.sql`); логика — `db/parts/40_functions_errors.sql`.

Матчинг **ярусный** (`match_tier`): на каждый `<item>` правила проверяются по возрастанию яруса, и первый ярус, давший хотя бы одно совпадение, закрывает поиск — правила нижних ярусов для этого item не применяются. Семантика ярусов: **1** — код + специфичный текстовый паттерн; **2** — только код (`match_pattern = '(?is).*'`, пустой `message` допустим — item без текста классифицируется правилом, а не «Код: X»); **3** — специфичный текстовый паттерн без кода; **4** — широкие текстовые фолбэки. Несколько совпадений внутри выигравшего яруса легальны и дедуплицируются; мультитиповость документа возникает только из нескольких `<item>` или нескольких совпадений одного яруса. Если правила молчат — code-fallback (возвращает те же канонические типы, что и правила соответствующих кодов), единый тип «Ошибка Schematron-валидации», «Код: X» (только для кодов, не покрытых ни одним правилом), «Неизвестная ошибка». Канонический тип всегда из словаря; детальный текст ошибки (включая номера schematron-правил) остаётся в `error_text`. Все типы контура ИЭМК начинаются с префикса `ИЭМК: `.

**Контур обмена** (`transactions.contour`: `РЭМД`/`ИЭМК`/NULL) — направление интеграции с ЕГИСЗ, вычисляется `exchange_contour()`: первично по `wsa:Action` payload'а (`urn:ihe:*` → ИЭМК, иной непустой → РЭМД), запасной признак — порт сервиса клиники в `LOGTEXT` (`:9921` → ИЭМК, `:9945` → РЭМД; конвенция настроек клиник, прочие порты контур не определяют). Приоритет всегда у action; порт — условный признак. На грейне сообщения хранится в `transactions.contour`; на грейне документа фиксируется в `document_attributes.contour` (последний непустой контур по строкам документа) и оттуда экспонируется в `rpt_network_errors.contour` — отчётный слой не читает message-грейн `transactions` напрямую.

Связь **тип → группа** — единый справочник `dim_error_type_group` (тип PK → категория), единственный источник истины. Это исключает «один тип → две группы». Каждый тип дополнительно несёт `responsibility` (кто устраняет причину: клиника / МИС / интегратор / РЭМД / смешанная) и `is_retryable` (лечится ли повторной отправкой) — см. [приложение](#зона-ответственности-и-повторяемость).

В `documents`:

| Поле | Содержание |
| ---- | ---------- |
| `error_types` | Канонические **типы** документа (один или несколько) через `·`; в БД хранится полным списком |
| `error_text` | Исходный текст из `<message>` **без редактирования** |

`rpt_error_breakdown` раскладывает `error_types` на атомарные строки (split по `·`): 1 ряд = 1 канонический тип на документ (поле `error_type`); категория, зона ответственности и повторяемость подтягиваются JOIN'ом к `dim_error_type_group`.

Перечень типов — [приложение](#приложение-типы-ошибок). Условия правил — [справочник](#справочник-активных-правил-классификации).

---

## DWH-модель

БД `dwh_egisz`. Схема — идемпотентный прогон `db/dwh_init.sql` (модули `db/parts/`).

| Слой | Объект | Grain / назначение |
| ---- | ------ | ------------------ |
| Состояние | `elt_state` | `last_logid` — курсор инкрементальной обработки |
| Raw | `exchangelog_raw` | Строка журнала как в источнике |
| Транзакции | `transactions` | Строка журнала + `xml_*` (parse-once) + `error_type` на callback + `contour` (РЭМД/ИЭМК, `exchange_contour`); `loaded_at` — момент ELT-загрузки (не путать с `ips_date`) |
| Факт | `documents` | Один экземпляр/версия СЭМД — одна строка (`dwh_id`); логическая группа версий — `document_group_id`, см. §«Версии и идентичность документа» |
| Атрибуты | `document_attributes` | Lineage клиники, host, mismatch JID, `request_msgid`, `contour` (контур обмена на грейне документа) |
| Справочники | `dim_organizations`, `dim_licenses`, `dim_semd_types`, `dim_document_status` | Клиники, лицензии, типы СЭМД, подписи статусов |
| Правила | `dim_error_rules` | `match_tier` (ярус 1–4) + `match_code` + `match_pattern` → `interpretation` |
| Тип→группа | `dim_error_type_group` | Канонический тип (PK) → категория + `responsibility` + `is_retryable` — единый источник истины |

Представления `rpt_*`:

| Представление | Содержание |
| ------------- | ---------- |
| `rpt_documents` | `documents` + `document_attributes` + справочники; **текущие версии** (`is_current_version`) |
| `rpt_document_versions` | то же, но все экземпляры/версии (полный аудит, включая superseded) |
| `rpt_documents_waiting` | `status = waiting` |
| `rpt_network_errors` | `status = network_error`; + `contour` — контур обмена (РЭМД/ИЭМК) из `document_attributes` |
| `rpt_error_breakdown` | Split `error_types` по `·` → атомарный канонический вид (`error_type` + `error_category` + `responsibility` + `is_retryable`) |
| `rpt_document_lineage` | OID / host / endpoint по документу |
| `rpt_clinic_semd_licenses` | Доступные клинике типы СЭМД: грейн (`clinic_jid`, `semd_code` = `EGISZ_LICENSES.KIND`), наименование из `dim_semd_types`, актуальность `MAX(modifydate)`, начало использования `MIN(bdate)` (источником пока не заполняется) |
| `rpt_health_*` | Свежесть и состояние контура (в т.ч. `rpt_health_versions` — наблюдаемость слоя версий) |

Имена колонок в Metabase Models:

| Префикс | Примеры |
| ------- | ------- |
| `semd_*` | `semd_code`, `semd_name`, `semd_local_uid`, `semd_emdr_id`, `semd_created_at` |
| `clinic_*` | `clinic_jid` (`bigint`), `clinic_name`, `clinic_jid_mismatch` |
| транспорт | `request_logid`, `result_logid`, `request_msgid`, `result_msgid`, `relates_to_msgid`, `logid` (= `COALESCE(result_logid, request_logid)`) |
| версии | `document_group_id`, `is_current_version`, `semd_version_number`, `supersedes_dwh_id` |
| без префикса | `status`, `status_label`, `error_types`, `error_type`, `error_category`, `error_text`, `ips_date`, `first_sent_at`, `delivery_seconds` |

Временные поля витрин:

| Поле | Смысл | Где в дашбордах |
| ---- | ----- | --------------- |
| `ips_date` | Дата обработки транспортом IPS (`EXCHANGELOG.CREATEDATE`): `COALESCE(last_callback_at, registered_at, first_sent_at)`; XML-дата создания (`semd_created_at`) сюда не входит | Фильтр периода «Обработано IPS» |
| `first_sent_at` | Поступление на прокси (первое IPS-событие отправки) | Карточка «Динамика документов по дням» (вкладка «Архив СЭМД») |
| `loaded_at` (`transactions`) | Момент загрузки строки журнала в ELT | Служебное; в `rpt_*` не экспонируется |

День периода берётся из полной даты инлайн (`ips_date::date`), без `AT TIME ZONE` — стек пиннится МСК (`report-timezone=Europe/Moscow`).

---

## Дашборды Metabase

Четыре JSON-дашборда в `metabase_dashboards/`
Провижининг — `metabase/setup-dashboards.sh` 
Модели — `metabase/sync-models.sh`


| Файл | Имя в Metabase | Аудитория | Содержание |
| ---- | -------------- | --------- | ---------- |
| `01_integration_egisz.json` | Интеграция с ЕГИСЗ | Поддержка, аналитики | Оперативный мониторинг, healthcheck, очередь, ошибки, архив |
| `05_executive.json` | Управленческий дашборд | Руководство | Вкладки: «Обзор» (NSM, MRR/ARR, здоровье сервиса, выручка под риском по `rpt_documents`), «Динамика по неделям» и «Динамика по месяцам» (SLI, p-карта, структура ошибок по `rpt_*_weekly` / `rpt_*_monthly`) |
| `07_client_service.json` | Клиентский дашборд. Мониторинг сервиса интеграции с ЕГИСЗ | Клиника | Обзор, ошибки регистрации, журнал документов, доступные типы СЭМД |
| `08_client_bianalytic.json` | Клиентский дашборд. BI-аналитика ЭМД | Клиника (BI) | Объёмы производства, доставка, пациенты/врачи |

### «Интеграция с ЕГИСЗ» (`01_integration_egisz.json`)

| Вкладка | Фокус | Ключевые карточки |
| ------- | ----- | ----------------- |
| Оперативный мониторинг | Срез за период по корпусу результатов | «Последние операции», «Статусы за период», «Топ по типу ошибки», «Успешность по типам СЭМД/клиникам», тепловая карта |
| Сервис интеграции | Healthcheck и сбои транспорта | «Контроль качества данных», тренды сетевых ошибок, «Последние сбои транспорта» |
| Отправленные | Очередь без callback | KPI зависших сегментов, «Очередь без ответа», топ клиник/СЭМД в очереди |
| Анализ ошибок | Структура отказов РЭМД | «Топ по типу ошибки», «Ошибки: тип × клиника», кросс-таблицы «% ошибок …» |
| Архив СЭМД | Исторический объём | «Динамика документов по дням» (`first_sent_at`), «Объём по клиникам», журнал «Архив СЭМД» |

### Клиентский дашборд (`07_client_service.json`, «Клиентский дашборд. Мониторинг сервиса интеграции с ЕГИСЗ»)

Обязательный фильтр `JID Клиники`. PII маскируется на уровне DWH. Публичная ссылка без авторизации.

| Вкладка | Фокус | Учёт «Отправлено» |
| ------- | ----- | ----------------- |
| Обзор | Объёмы и статусы регистрации, динамика, топ СЭМД | Только корпус результатов |
| Ошибки регистрации ЭМД | Категории, типы, динамика, топ СЭМД по ошибкам (`rpt_error_breakdown`) | Только корпус результатов |
| Документы | Журнал с финальным ответом РЭМД | Отдельная карточка «Отправленные — клиент» (`rpt_documents_waiting`) |
| Доступные типы СЭМД | Типы СЭМД, доступные клинике по лицензиям (`rpt_clinic_semd_licenses`, пара JID + KIND) | Не применяется (справочник) |

Дашборд `08_client_bianalytic.json`: карточка «Документов за период — BI» считает все строки `rpt_documents` (включая `waiting`); доли «% успеха» — только по финальным статусам (`success`, `async_error`, `network_error`).

### Провижининг и публичная ссылка

| Настройка | Механизм |
| --------- | -------- |
| Локализация и формат | `setup-dashboards.sh` → `ensure_localization_defaults`: `site-locale=ru`, `report-timezone=Europe/Moscow`, 24ч, ₽, кеш адаптивной TTL (`PUT /api/cache`) |
| Публичная страница | `enable-public-sharing` + ссылка `${METABASE_URL}/public/dashboard/<uuid>`; флаг `METABASE_PUBLIC_CLIENT_DASHBOARD` (по умолчанию `true`) |
| Фильтры на публичной ссылке | `JID Клиники` и `Клиника` (`clinic_label`) — связанные field filters; `JID` также через URL; заголовок подставляет `{{clinic_label}}`; скрыть фильтр клиники: `#hide_parameters=clinic_label` |

Metabase Models → витрины DWH:

| Модель | Витрина | Grain |
| ------ | ------- | ----- |
| Документы (`01_documents`) | `rpt_documents` | 1 строка = 1 логический документ (текущая версия) |
| Разбивка ошибок (`02_error_breakdown`) | `rpt_error_breakdown` | 1 строка = 1 тип ошибки на документ |
| Очередь без ответа (`03_no_response`) | `rpt_documents_waiting` | 1 строка = 1 документ без callback |
| Сбои транспорта (`04_network_errors`) | `rpt_network_errors` | 1 строка = 1 сетевая ошибка |

Простые срезы и drill-through — через Models и Query Builder; pivot, stacked и сложный SQL — native-запросы к `rpt_*`. Дефолты фильтров — `field_filter_defaults.yaml`.

---

## Запуск контуров

### Предварительные требования

| Компонент | Требование |
| --------- | ---------- |
| Kubernetes | Кластер с `kubectl` и `helm` (локально — Docker Desktop, Kubernetes включён) |
| PostgreSQL | БД `dwh_egisz`, роль `egisz` |
| Схема DWH | Идемпотентный прогон: `psql -U postgres -d dwh_egisz -v ON_ERROR_STOP=1 -f db/dwh_init.sql` |
| Секреты k8s | `up.ps1` копирует `k8s/**/*.example.yaml` → `*secret*.yaml`, если целевых файлов ещё нет |
| Подключения Airflow | `k8s/airflow/egisz-connections.json` (создаётся из `*.example.json`); `up.ps1` заносит их в Airflow как Connections `dwh_egisz_pg` / `proxy_egisz_fb` |

### Команды

```
.\up.ps1                               # Airflow + Metabase
.\up.ps1 -Action Airflow               # только Airflow
.\up.ps1 -Action Metabase              # только Metabase
.\up.ps1 -Action Metabase-Provisioning # переимпорт дашбордов и моделей Metabase
python scripts/export_dashboard.py     # выгрузка дашбордов с живого Metabase в metabase_dashboards/
.\scripts\build_external_bundle.ps1    # бандлы для переноса на внешний контур — см. deploy/README.md
.\up.ps1 -Action Stop                  # удаление namespace egisz-bi
.\up.ps1 -Action Stop-Airflow          # остановка Airflow (данные на PVC сохраняются)
.\up.ps1 -Action Stop-Metabase         # остановка Metabase (данные на PVC сохраняются)
```

Airflow UI — `http://localhost:8080`, Metabase — `http://localhost:3000`, namespace `egisz-bi`.

---

## Глоссарий

| Аббревиатура              | Расшифровка                                                                   |
| ------------------------- | ----------------------------------------------------------------------------- |
| ЕГИСЗ                     | Единая государственная информационная система в сфере здравоохранения         |
| РЭМД                      | Реестр электронных медицинских документов (подсистема ЕГИСЗ)                  |
| СЭМД                      | Структурированный электронный медицинский документ (CDA-документ для РЭМД)    |
| МО                        | Медицинская организация (клиника)                                             |
| `JID`                     | Идентификатор клиники в журнале шлюза (`bigint` в DWH)                        |
| МИС                       | Медицинская информационная система клиники                                    |
| ИС                        | Информационная система (в контексте РЭМД — МИС, зарегистрированная в реестре) |
| ФРМР                      | Федеральный регистр медицинских работников                                    |
| ФРМО                      | Федеральный реестр медицинских организаций                                    |
| ГИП                       | Главный индекс пациента                                                       |
| НСИ                       | Нормативно-справочная информация Минздрава (справочники ЕГИСЗ)               |
| ЭП                        | Электронная подпись                                                           |
| УЦ                        | Удостоверяющий центр (выдаёт сертификаты ЭП)                                  |
| CRL / OCSP                | Списки / протокол проверки отозванных сертификатов                            |
| ДУЛ                       | Документ, удостоверяющий личность                                             |
| СНИЛС                     | Страховой номер индивидуального лицевого счёта                                |
| ОГРН / ИНН / КПП          | Реквизиты юридического лица                                                   |
| OID                       | Object Identifier (для справочников НСИ)                                      |
| `emdrId`                  | Идентификатор зарегистрированного документа в РЭМД                            |
| `localUid`                | CDA `ClinicalDocument/id` — идентификатор **версии** документа в МИС; меняется при правке/ре-выгрузке ⇒ `dwh_id` |
| `setId` / `versionNumber` | CDA-ключ набора версий и номер версии; в журнал не попадают (нет тела СЭМД)    |
| `documentNumber` / `doc_number` | Номер протокола/ИБ в МИС (`PROTOCOLID`, тег `documentNumber`); ключ группировки версий вместе с `jid` и `semd_code` |
| `document_group_id`       | Ключ логического документа: `d:jid\|semd\|docnum` при группе версий, иначе `dwh_id` |
| `messageId` / `relatesTo` | Корреляционные идентификаторы SOAP-сообщений                                  |
| callback                  | Асинхронный ответ РЭМД на исходящее сообщение                                |
| DWH                       | Аналитическое хранилище (`dwh_egisz`)                                         |

---

## Приложение: типы ошибок

Классификатор сопоставляет `code` и текст `<message>` из callback с правилами DWH (ярусный матчинг — см. §«Классификация ошибок»). Несколько типов в одном документе разделяются `·`. Мнемоники кодов РЭМД сверяются со справочником НСИ «РЭМД. Классификатор кодов сообщений» (OID `1.2.643.5.1.13.13.99.2.305`, nsi.rosminzdrav.ru): для кодов регистрационного пути, ещё не встречавшихся в журнале, заведены defensive-правила яруса 2 — непокрытые коды видны по остатку «Код: X». Контур ИЭМК покрыт аналогично: живые платформенные коды (`XDS.CDA.*`, `XDSDictionaryValidationError` и др.) плюс стандартные коды IHE ITI TF-3 регистрационного пути ITI-41/42; внутренние коды платформы (`CRE-NNN`, `PAT-NNN`) приходят в `codeContext`.

| Категория | Типы ошибок (`error_type`) |
| --- | --- |
| Данные пациента | Адрес пациента не найден в ФИАС · Адрес пациента не соответствует ФИАС · Данные пациента не соответствуют ГИП · Дата рождения пациента не заполнена или некорректна · Документ, удостоверяющий личность пациента: некорректные реквизиты · Не указан адрес пациента · Не указана серия документа, удостоверяющего личность · Не удалось создать пациента в ГИП · Неверный формат или контрольная сумма СНИЛС · Пациент не найден в ГИП · Пациент уже зарегистрирован в ГИП · Пол пациента в ЭМД не соответствует данным ЕГИСЗ · Получатель из запроса не найден в СЭМД · Сведения о пациенте отсутствуют или дублируются в документе · СНИЛС не найден или не соответствует данным пациента/медработника · СНИЛС пациента не заполнен или некорректен · СНИЛС пациента обязателен для данного вида документов · Требуются дополнительные сведения о пациенте (ГИП) · ФИО пациента в ЭМД не соответствует данным ЕГИСЗ · ФИО пациента не заполнено или некорректно |
| Данные медработника | Данные медработника не соответствуют ФРМР · Данные организации автора документа не заполнены · Должность врача не соответствует данным ФРМР · Должность подписанта не соответствует роли подписи · Имя врача не соответствует данным СЭМД · Карточка медработника не найдена в ФРМР · Медработник не найден в ФРМР · Организация подписанта не соответствует организации документа · Отчество врача не соответствует данным СЭМД · Подписант из сертификата не найден в ФРМР · СНИЛС автора (врача) не заполнен или некорректен · Специальность врача не соответствует справочнику НСИ · У подписанта не указана специальность |
| Ошибки структуры и валидации | Данные заверителя документа не заполнены или некорректны · Данные хранителя документа не заполнены · Дата/время создания документа не заполнены или некорректны · Дополнительные атрибуты документа не прошли валидацию · Код типа документа не соответствует справочнику НСИ · Некорректно заполнен телефон · Организация не привязана к РМИС · Ошибка Schematron-валидации · Ошибка XSD-валидации XML · Ошибка разбора XML-структуры документа · Формат файла не соответствует требованиям вида документа |
| Ошибки справочника НСИ | Код отсутствует в справочнике НСИ · Наименование не соответствует справочнику НСИ · Неактуальная версия справочника НСИ · Ошибка справочника НСИ |
| Ошибки регистрации в РЭМД | Вид документа не актуален на дату создания · Дата подписи МО позже даты поступления запроса на регистрацию · Дата создания документа в ЭМД не совпадает с датой в запросе на регистрацию · Дата создания документа позже даты регистрации · Для данного вида ЭМД запрещена регистрация новых версий · Документ аннулирован · Документ не найден в РЭМД · Документ уже зарегистрирован в РЭМД · Доступ к операции запрещён в РЭМД · Дублирующий запрос · Идентификатор документа в ЭМД не совпадает с идентификатором в запросе на регистрацию · Метаописание документа не соответствует зарегистрированному в РЭМД · Не удалось зарегистрировать новую версию документа · Не удалось связать документ с записью РЭМД · Неверный идентификатор документа РЭМД · Неверный идентификатор сообщения (messageId) · Неверный формат запроса · Неподдерживаемый тип СЭМД в РЭМД · Подразделение или запись справочника не найдены на дату документа · Регистрируемая связь документов уже существует · Роль подписанта не предусмотрена для вида документа на дату · Структурное подразделение (providerOrganization) в СЭМД не совпадает с запросом на регистрацию · Структурное подразделение (representedOrganization) в СЭМД не совпадает с запросом на регистрацию |
| Ошибки организации / ИС | ИС зарегистрирована в РЭМД, но не активна: проверьте уведомления и переподключение ИС · ИС не зарегистрирована в РЭМД или указаны неверные регистрационные данные · Лицензия организации не найдена · Несоответствие данных организации в ФРМО · Организация не действует в ФРМО на дату документа · Организация не зарегистрирована в РЭМД · Организация не найдена в реестре РЭМД · Организация не найдена в ФРМО · Ошибки организации · Подразделение организации не найдено в ФРМО · Регион ИС не соответствует региону организации |
| Ошибки получения файла ЭМД | Запрашиваемая запись ЭМД не найдена в предоставляющей ИС · ИС не передала файл ЭМД в ответе getDocumentFile · Не удалось получить файл ЭМД из предоставляющей ИС · Сервис предоставляющей ИС недоступен: проверьте доступность getDocumentFile |
| Ошибки ЭП и сертификатов | В контейнере подписи более одного подписанта · В подписи отсутствует сертификат подписанта · Данные подписи не соответствуют данным документа · Дублирование подписи в документе · Не удалось декодировать электронную подпись · Не удалось проверить электронную подпись · Недействительный сертификат подписи · Недоступен сервис проверки статуса сертификата (CRL/OCSP) · Неподдерживаемый алгоритм электронной подписи · Неподдерживаемый формат электронной подписи · Подпись не соответствует содержимому документа · Подпись организации не соответствует требованиям вида документа · Подпись роли не соответствует требованиям РЭМД · Сертификат не предназначен для подписания документов · Сертификат подписи недействителен на дату создания документа · Сертификат ЭП истёк · Сертификат ЭП отозван · Срок действия сертификата организации истек · Электронная подпись отсутствует |
| Технические ошибки РЭМД | Недоступен сервис проверки подписи (УЦ) на стороне РЭМД · Превышен лимит запросов к РЭМД · РЭМД не смог обработать запрос · Таймаут асинхронной обработки на стороне РЭМД · Техническая ошибка на стороне РЭМД |
| Ошибки ИЭМК | ИЭМК: внутренняя ошибка реестра · ИЭМК: внутренняя ошибка репозитория · ИЭМК: данные не соответствуют справочнику НСИ · ИЭМК: документ уже зарегистрирован · ИЭМК: дублирующийся идентификатор в пакете · ИЭМК: замена версии отклонена (документ уже заменён) · ИЭМК: замена версии отклонена (другая организация) · ИЭМК: заменяемый документ не найден (замена версии) · ИЭМК: некорректный идентификатор документа · ИЭМК: неверный идентификатор репозитория · ИЭМК: ошибка валидации структуры CDA · ИЭМК: ошибка метаданных документа · ИЭМК: ошибка обработки CDA · ИЭМК: пациент не определён · ИЭМК: повторная загрузка с изменённым содержимым · ИЭМК: сервис временно недоступен · ИЭМК: состав пакета не согласован (документы/метаданные) |
| Ошибки связи | Сетевая ошибка |
| Прочие | Неизвестная ошибка |

### Зона ответственности и повторяемость

Каждый тип в `dim_error_type_group` несёт `responsibility` (кто устраняет причину: `клиника` / `МИС` / `интегратор` / `РЭМД` / `смешанная`) и `is_retryable` (устраняется ли повторной отправкой без правки данных). Обе колонки экспонируются в `rpt_error_breakdown`. Значения по умолчанию — по категории:

| Категория | Зона ответственности | Устраняется повтором |
| --- | --- | --- |
| Данные пациента | клиника | нет |
| Данные медработника | клиника | нет |
| Ошибки структуры и валидации | МИС | нет |
| Ошибки справочника НСИ | клиника | нет |
| Ошибки регистрации в РЭМД | смешанная | нет |
| Ошибки организации / ИС | клиника | нет |
| Ошибки получения файла ЭМД | МИС | да |
| Ошибки ЭП и сертификатов | клиника | нет |
| Технические ошибки РЭМД | РЭМД | да |
| Ошибки ИЭМК | смешанная | нет |
| Ошибки связи | интегратор | да |
| Прочие | смешанная | нет |

Точечные переопределения по типам:

| Тип ошибки | Категория | Зона ответственности | Устраняется повтором |
| --- | --- | --- | --- |
| ИЭМК: внутренняя ошибка реестра | Ошибки ИЭМК | РЭМД | да |
| ИЭМК: внутренняя ошибка репозитория | Ошибки ИЭМК | РЭМД | да |
| ИЭМК: данные не соответствуют справочнику НСИ | Ошибки ИЭМК | клиника | нет |
| ИЭМК: документ уже зарегистрирован | Ошибки ИЭМК | МИС | нет |
| ИЭМК: дублирующийся идентификатор в пакете | Ошибки ИЭМК | МИС | нет |
| ИЭМК: замена версии отклонена (документ уже заменён) | Ошибки ИЭМК | МИС | нет |
| ИЭМК: заменяемый документ не найден (замена версии) | Ошибки ИЭМК | МИС | нет |
| ИЭМК: некорректный идентификатор документа | Ошибки ИЭМК | МИС | нет |
| ИЭМК: неверный идентификатор репозитория | Ошибки ИЭМК | интегратор | нет |
| ИЭМК: ошибка валидации структуры CDA | Ошибки ИЭМК | МИС | нет |
| ИЭМК: ошибка метаданных документа | Ошибки ИЭМК | МИС | нет |
| ИЭМК: ошибка обработки CDA | Ошибки ИЭМК | РЭМД | да |
| ИЭМК: пациент не определён | Ошибки ИЭМК | клиника | нет |
| ИЭМК: повторная загрузка с изменённым содержимым | Ошибки ИЭМК | МИС | нет |
| ИЭМК: сервис временно недоступен | Ошибки ИЭМК | РЭМД | да |
| ИЭМК: состав пакета не согласован (документы/метаданные) | Ошибки ИЭМК | МИС | нет |
| ИС зарегистрирована в РЭМД, но не активна: проверьте уведомления и переподключение ИС | Ошибки организации / ИС | интегратор | нет |
| ИС не зарегистрирована в РЭМД или указаны неверные регистрационные данные | Ошибки организации / ИС | интегратор | нет |
| Сервис предоставляющей ИС недоступен: проверьте доступность getDocumentFile | Ошибки получения файла ЭМД | интегратор | да |
| Дата подписи МО позже даты поступления запроса на регистрацию | Ошибки регистрации в РЭМД | МИС | нет |
| Дата создания документа в ЭМД не совпадает с датой в запросе на регистрацию | Ошибки регистрации в РЭМД | МИС | нет |
| Документ уже зарегистрирован в РЭМД | Ошибки регистрации в РЭМД | МИС | нет |
| Дублирующий запрос | Ошибки регистрации в РЭМД | МИС | нет |
| Идентификатор документа в ЭМД не совпадает с идентификатором в запросе на регистрацию | Ошибки регистрации в РЭМД | МИС | нет |
| Метаописание документа не соответствует зарегистрированному в РЭМД | Ошибки регистрации в РЭМД | МИС | нет |
| Неверный идентификатор документа РЭМД | Ошибки регистрации в РЭМД | МИС | нет |
| Неверный формат запроса | Ошибки регистрации в РЭМД | МИС | нет |
| Структурное подразделение (providerOrganization) в СЭМД не совпадает с запросом на регистрацию | Ошибки регистрации в РЭМД | МИС | нет |
| Структурное подразделение (representedOrganization) в СЭМД не совпадает с запросом на регистрацию | Ошибки регистрации в РЭМД | МИС | нет |
| Дата создания документа позже даты регистрации | Ошибки регистрации в РЭМД | МИС | нет |
| Неверный идентификатор сообщения (messageId) | Ошибки регистрации в РЭМД | МИС | нет |
| Не удалось декодировать электронную подпись | Ошибки ЭП и сертификатов | МИС | нет |
| Неподдерживаемый формат электронной подписи | Ошибки ЭП и сертификатов | МИС | нет |
| Недоступен сервис проверки статуса сертификата (CRL/OCSP) | Ошибки ЭП и сертификатов | РЭМД | да |
| Регион ИС не соответствует региону организации | Ошибки организации / ИС | интегратор | нет |
| Превышен лимит запросов к РЭМД | Технические ошибки РЭМД | интегратор | да |
| Не удалось создать пациента в ГИП | Данные пациента | РЭМД | да |

### Справочник активных правил классификации

Порядок применения: ярусы 1→4 (первый ярус с совпадением побеждает; несколько совпадений внутри яруса дедуплицируются) → code-fallback → эвристика Schematron → «Неизвестная ошибка». Точные regex — в `db/parts/30_error_rules.sql`.

| Ярус | Код правила | Тип ошибки | Категория | Условие сопоставления |
| --- | --- | --- | --- | --- |
| 1 | `patient_snils_mismatch_request` | СНИЛС пациента не заполнен или некорректен | Данные пациента | код fault = `VALIDATION_ERROR`; текст `<message>` ~* `(?is)СНИЛС пациента в ЭМД \[.*?\] отличается` |
| 1 | `runtime_check_unavailable` | РЭМД не смог обработать запрос | Технические ошибки РЭМД | код fault = `RUNTIME_ERROR`; текст `<message>` ~* `(?is)Не уда(е|ё)тся про(из)?вести проверку` |
| 1 | `runtime_frmr_check_unavailable` | РЭМД не смог обработать запрос | Технические ошибки РЭМД | код fault = `RUNTIME_ERROR`; текст `<message>` ~* `(?is)провер.*ФРМР` |
| 1 | `runtime_request_processing` | РЭМД не смог обработать запрос | Технические ошибки РЭМД | код fault = `RUNTIME_ERROR`; текст `<message>` ~* `(?is)Невозможно обработать запрос` |
| 1 | `schematron_author_snils` | СНИЛС автора (врача) не заполнен или некорректен | Данные медработника | код fault = `VALIDATION_ERROR`; текст `<message>` ~* `(?is)(assignedAuthor.*(SNILS|СНИЛС|snils)|author.*(СНИЛС|snils))` |
| 1 | `schematron_author_specialty` | Специальность врача не соответствует справочнику НСИ | Данные медработника | код fault = `VALIDATION_ERROR`; текст `<message>` ~* `(?is)(assignedAuthor.*code.*codeSystem|assignedAuthor.*specialit|специальност.*автор|автор.*специальност)` |
| 1 | `schematron_creation_time` | Дата/время создания документа не заполнены или некорректны | Ошибки структуры и валидации | код fault = `VALIDATION_ERROR`; текст `<message>` ~* `(?is)(creationTime.*(не заполнен|некорректн|не указан|обязател))` |
| 1 | `schematron_custodian` | Данные хранителя документа не заполнены | Ошибки структуры и валидации | код fault = `VALIDATION_ERROR`; текст `<message>` ~* `(?is)(custodian|representedCustodianOrganization)` |
| 1 | `schematron_doc_code` | Код типа документа не соответствует справочнику НСИ | Ошибки структуры и валидации | код fault = `VALIDATION_ERROR`; текст `<message>` ~* `(?is)(ClinicalDocument/code|тип документа.*(справочник|OID|codeSystem))` |
| 1 | `schematron_legal_auth` | Данные заверителя документа не заполнены или некорректны | Ошибки структуры и валидации | код fault = `VALIDATION_ERROR`; текст `<message>` ~* `(?is)legalAuthenticator` |
| 1 | `schematron_org_not_linked_rmis` | Организация не привязана к РМИС | Ошибки структуры и валидации | код fault = `VALIDATION_ERROR`; текст `<message>` ~* `(?is)не привязана к РМИС` |
| 1 | `schematron_org_repr` | Данные организации автора документа не заполнены | Данные медработника | код fault = `VALIDATION_ERROR`; текст `<message>` ~* `(?is)(assignedAuthor.*representedOrganization|representedOrganization.*author)` |
| 1 | `schematron_patient_addr_generic` | Не указан адрес пациента | Данные пациента | код fault = `VALIDATION_ERROR`; текст `<message>` ~* `(?is)patientRole.*addr` |
| 1 | `schematron_patient_address_type` | Не указан адрес пациента | Данные пациента | код fault = `VALIDATION_ERROR`; текст `<message>` ~* `(?is)(Schematron|схематрон).*patientRole.*addr.*address:Type` |
| 1 | `schematron_patient_birth` | Дата рождения пациента не заполнена или некорректна | Данные пациента | код fault = `VALIDATION_ERROR`; текст `<message>` ~* `(?is)(patientRole.*birthTime|birthTime.*patient)` |
| 1 | `schematron_patient_name` | ФИО пациента не заполнено или некорректно | Данные пациента | код fault = `VALIDATION_ERROR`; текст `<message>` ~* `(?is)(patientRole.*(name|given|family)|(given|family).*patientRole)` |
| 1 | `schematron_patient_snils` | СНИЛС пациента не заполнен или некорректен | Данные пациента | код fault = `VALIDATION_ERROR`; текст `<message>` ~* `(?is)(patientRole.*(SNILS|СНИЛС)|patient.*(SNILS|СНИЛС))` |
| 1 | `schematron_telecom_missing` | Некорректно заполнен телефон | Ошибки структуры и валидации | код fault = `VALIDATION_ERROR`; текст `<message>` ~* `(?is)(telecom).*(не пустым значением|@value)|Ошибка заполнения номера телефона` |
| 1 | `signature_metadata_certificate` | Подписант из сертификата не найден в ФРМР | Данные медработника | код fault = `VALUE_MISMATCH_METADATA_AND_CERTIFICATE`; текст `<message>` ~* `(?is)не найдена актуальная.*карточка МР` |
| 1 | `validation_invalid_name_chars` | ФИО пациента не заполнено или некорректно | Данные пациента | код fault = `VALIDATION_ERROR`; текст `<message>` ~* `(?is)Недопустимые символы в имени` |
| 1 | `xds_document_unique_id_rplc` | ИЭМК: заменяемый документ не найден (замена версии) | Ошибки ИЭМК | код fault = `XDSDOCUMENTUNIQUEIDERROR`; текст `<message>` ~* `(?is)\yRPLC\y|targetId.*not found` |
| 2 | `access_denied_remd` | Доступ к операции запрещён в РЭМД | Ошибки регистрации в РЭМД | код fault = `ACCESS_DENIED`; текст `<message>` — любой |
| 2 | `additional_info_required_code` | Требуются дополнительные сведения о пациенте (ГИП) | Данные пациента | код fault = `ADDITIONAL_INFO_REQUIRED`; текст `<message>` — любой |
| 2 | `aoguid_different_code` | Адрес пациента не соответствует ФИАС | Данные пациента | код fault = `AOGUID_DIFFERENT`; текст `<message>` — любой |
| 2 | `aoguid_not_found_code` | Адрес пациента не найден в ФИАС | Данные пациента | код fault = `AOGUID_NOT_FOUND`; текст `<message>` — любой |
| 2 | `async_response_timeout_code` | Таймаут асинхронной обработки на стороне РЭМД | Технические ошибки РЭМД | код fault = `ASYNC_RESPONSE_TIMEOUT`; текст `<message>` — любой |
| 2 | `attribute_not_found_code` | Метаописание документа не соответствует зарегистрированному в РЭМД | Ошибки регистрации в РЭМД | код fault = `ATTRIBUTE_NOT_FOUND`; текст `<message>` — любой |
| 2 | `ca_inaccessibility_code` | Недоступен сервис проверки подписи (УЦ) на стороне РЭМД | Технические ошибки РЭМД | код fault = `CA_INACCESSIBILITY`; текст `<message>` — любой |
| 2 | `ca_unavailable_code` | Недоступен сервис проверки подписи (УЦ) на стороне РЭМД | Технические ошибки РЭМД | код fault = `CA_UNAVAILABLE`; текст `<message>` — любой |
| 2 | `can_not_associate_code` | Не удалось связать документ с записью РЭМД | Ошибки регистрации в РЭМД | код fault = `CAN_NOT_ASSOCIATE`; текст `<message>` — любой |
| 2 | `cant_reg_version_code` | Не удалось зарегистрировать новую версию документа | Ошибки регистрации в РЭМД | код fault = `CANT_REG_VERSION`; текст `<message>` — любой |
| 2 | `cert_org_validity_expired` | Срок действия сертификата организации истек | Ошибки ЭП и сертификатов | код fault = `CANT_BUILD_CERT_CHAIN_TO_ACCREDITED_CA_CERT`; текст `<message>` — любой |
| 2 | `digest_mismatch_code` | Подпись не соответствует содержимому документа | Ошибки ЭП и сертификатов | код fault = `DIGEST_MISMATCH`; текст `<message>` — любой |
| 2 | `doc_date_cert_not_after_code` | Сертификат подписи недействителен на дату создания документа | Ошибки ЭП и сертификатов | код fault = `DOC_DATE_MISMATCH_CERT_NOT_AFTER`; текст `<message>` — любой |
| 2 | `doc_date_cert_not_before_code` | Сертификат подписи недействителен на дату создания документа | Ошибки ЭП и сертификатов | код fault = `DOC_DATE_MISMATCH_CERT_NOT_BEFORE`; текст `<message>` — любой |
| 2 | `doctor_patronymic_mismatch` | Отчество врача не соответствует данным СЭМД | Данные медработника | код fault = `INVALID_DOCTOR_PATRONYMIC`; текст `<message>` — любой |
| 2 | `doctor_position_frmr` | Должность врача не соответствует данным ФРМР | Данные медработника | код fault = `PERSON_POST_IN_FRMR_MISMATCH`; текст `<message>` — любой |
| 2 | `document_already_registered` | Документ уже зарегистрирован в РЭМД | Ошибки регистрации в РЭМД | код fault = `NOT_UNIQUE_PROVIDED_ID`; текст `<message>` — любой |
| 2 | `document_file_get_error` | Не удалось получить файл ЭМД из предоставляющей ИС | Ошибки получения файла ЭМД | код fault = `GET_DOCUMENT_FILE_ERROR`; текст `<message>` — любой |
| 2 | `document_file_not_sent` | ИС не передала файл ЭМД в ответе getDocumentFile | Ошибки получения файла ЭМД | код fault = `FILE_WAS_NOT_SENT`; текст `<message>` — любой |
| 2 | `document_kind_not_actual` | Вид документа не актуален на дату создания | Ошибки регистрации в РЭМД | код fault = `NO_DOCUMENT_KIND_ON_DATE`; текст `<message>` — любой |
| 2 | `document_metadata_mismatch` | Метаописание документа не соответствует зарегистрированному в РЭМД | Ошибки регистрации в РЭМД | код fault = `ATTRIBUTE_MISMATCH`; текст `<message>` — любой |
| 2 | `document_not_found_remd` | Документ не найден в РЭМД | Ошибки регистрации в РЭМД | код fault = `DOCUMENT_NOT_FOUND`; текст `<message>` — любой |
| 2 | `document_provider_response_error` | Не удалось получить файл ЭМД из предоставляющей ИС | Ошибки получения файла ЭМД | код fault = `RMIS_ERROR`; текст `<message>` — любой |
| 2 | `document_provider_unavailable` | Сервис предоставляющей ИС недоступен: проверьте доступность getDocumentFile | Ошибки получения файла ЭМД | код fault = `MIS_NOT_AVAILABLE`; текст `<message>` — любой |
| 2 | `document_registry_item_missing` | Запрашиваемая запись ЭМД не найдена в предоставляющей ИС | Ошибки получения файла ЭМД | код fault = `REGISTRY_ITEM_NOT_FOUND`; текст `<message>` — любой |
| 2 | `duplicate_request` | Дублирующий запрос | Ошибки регистрации в РЭМД | код fault = `DUPLICATE_REQUEST`; текст `<message>` — любой |
| 2 | `houseguid_not_found_code` | Адрес пациента не найден в ФИАС | Данные пациента | код fault = `HOUSEGUID_NOT_FOUND`; текст `<message>` — любой |
| 2 | `inconsistent_digests_code` | Подпись не соответствует содержимому документа | Ошибки ЭП и сертификатов | код fault = `INCONSISTENT_DIGESTS`; текст `<message>` — любой |
| 2 | `internal_error_code` | Техническая ошибка на стороне РЭМД | Технические ошибки РЭМД | код fault = `INTERNAL_ERROR`; текст `<message>` — любой |
| 2 | `invalid_cert_key_usage_code` | Сертификат не предназначен для подписания документов | Ошибки ЭП и сертификатов | код fault = `INVALID_CERT_KEY_USAGE`; текст `<message>` — любой |
| 2 | `invalid_content_code` | Формат файла не соответствует требованиям вида документа | Ошибки структуры и валидации | код fault = `INVALID_CONTENT`; текст `<message>` — любой |
| 2 | `invalid_dictionary_version_code` | Неактуальная версия справочника НСИ | Ошибки справочника НСИ | код fault = `INVALID_DICTIONARY_VERSION`; текст `<message>` — любой |
| 2 | `invalid_doc_content_type_code` | Формат файла не соответствует требованиям вида документа | Ошибки структуры и валидации | код fault = `INVALID_DOC_CONTENT_TYPE`; текст `<message>` — любой |
| 2 | `invalid_doctor_name_code` | Имя врача не соответствует данным СЭМД | Данные медработника | код fault = `INVALID_DOCTOR_NAME`; текст `<message>` — любой |
| 2 | `invalid_emdr_id` | Неверный идентификатор документа РЭМД | Ошибки регистрации в РЭМД | код fault = `INVALID_EMDR_ID`; текст `<message>` — любой |
| 2 | `invalid_pluggable_attrs_code` | Дополнительные атрибуты документа не прошли валидацию | Ошибки структуры и валидации | код fault = `INVALID_PLUGGABLE_ATTRS`; текст `<message>` — любой |
| 2 | `invalid_request_format` | Неверный формат запроса | Ошибки регистрации в РЭМД | код fault = `INVALID_REQUEST_FORMAT`; текст `<message>` — любой |
| 2 | `invalid_snils_code` | Неверный формат или контрольная сумма СНИЛС | Данные пациента | код fault = `INVALID_SNILS`; текст `<message>` — любой |
| 2 | `mis_error_code` | Не удалось получить файл ЭМД из предоставляющей ИС | Ошибки получения файла ЭМД | код fault = `MIS_ERROR`; текст `<message>` — любой |
| 2 | `multiple_signers_code` | В контейнере подписи более одного подписанта | Ошибки ЭП и сертификатов | код fault = `MULTIPLE_SIGNERS`; текст `<message>` — любой |
| 2 | `no_department_code` | Подразделение организации не найдено в ФРМО | Ошибки организации / ИС | код fault = `NO_DEPARTMENT`; текст `<message>` — любой |
| 2 | `no_end_entity_certificate_code` | В подписи отсутствует сертификат подписанта | Ошибки ЭП и сертификатов | код fault = `NO_END_ENTITY_CERTIFICATE`; текст `<message>` — любой |
| 2 | `no_org_on_date_code` | Организация не действует в ФРМО на дату документа | Ошибки организации / ИС | код fault = `NO_ORG_ON_DATE`; текст `<message>` — любой |
| 2 | `no_role_policy_on_date_code` | Роль подписанта не предусмотрена для вида документа на дату | Ошибки регистрации в РЭМД | код fault = `NO_ROLE_POLICY_ON_DATE`; текст `<message>` — любой |
| 2 | `no_signature_code` | Электронная подпись отсутствует | Ошибки ЭП и сертификатов | код fault = `NO_SIGNATURE`; текст `<message>` — любой |
| 2 | `no_snils_code` | СНИЛС пациента обязателен для данного вида документов | Данные пациента | код fault = `NO_SNILS`; текст `<message>` — любой |
| 2 | `no_speciality_code` | У подписанта не указана специальность | Данные медработника | код fault = `NO_SPECIALITY`; текст `<message>` — любой |
| 2 | `not_unique_association_code` | Регистрируемая связь документов уже существует | Ошибки регистрации в РЭМД | код fault = `NOT_UNIQUE_ASSOCIATION`; текст `<message>` — любой |
| 2 | `nsi_dictionary_code` | Код отсутствует в справочнике НСИ | Ошибки справочника НСИ | код fault = `INVALID_ELEMENT_VALUE_CODE`; текст `<message>` — любой |
| 2 | `nsi_dictionary_name` | Наименование не соответствует справочнику НСИ | Ошибки справочника НСИ | код fault = `INVALID_ELEMENT_VALUE_NAME`; текст `<message>` — любой |
| 2 | `nsi_dictionary_version` | Неактуальная версия справочника НСИ | Ошибки справочника НСИ | код fault = `INVALID_DICTIONARY_OID`; текст `<message>` — любой |
| 2 | `object_not_found` | Подразделение или запись справочника не найдены на дату документа | Ошибки регистрации в РЭМД | код fault = `OBJECT_NOT_FOUND`; текст `<message>` — любой |
| 2 | `org_not_found_in_frmo_code` | Организация не найдена в ФРМО | Ошибки организации / ИС | код fault = `ORG_NOT_FOUND_IN_FRMO`; текст `<message>` — любой |
| 2 | `org_signature_occurrence_code` | Подпись организации не соответствует требованиям вида документа | Ошибки ЭП и сертификатов | код fault = `ORG_SIGNATURE_OCCURRENCE_MISMATCH`; текст `<message>` — любой |
| 2 | `organization_license_not_found` | Лицензия организации не найдена | Ошибки организации / ИС | код fault = `ORGANIZATION_LICENSE_NOT_FOUND`; текст `<message>` — любой |
| 2 | `organization_not_found` | Организация не найдена в реестре РЭМД | Ошибки организации / ИС | код fault = `ORGANIZATION_NOT_FOUND`; текст `<message>` — любой |
| 2 | `organization_not_registered` | Организация не зарегистрирована в РЭМД | Ошибки организации / ИС | код fault = `ORGANIZATION_NOT_REGISTERED`; текст `<message>` — любой |
| 2 | `patient_already_registered_code` | Пациент уже зарегистрирован в ГИП | Данные пациента | код fault = `PATIENT_ALREADY_REGISTERED`; текст `<message>` — любой |
| 2 | `patient_creation_error_code` | Не удалось создать пациента в ГИП | Данные пациента | код fault = `PATIENT_CREATION_ERROR`; текст `<message>` — любой |
| 2 | `patient_data_gip` | Данные пациента не соответствуют ГИП | Данные пациента | код fault = `PATIENT_MPI_MISMATCH`; текст `<message>` — любой |
| 2 | `patient_not_found_code` | Пациент не найден в ГИП | Данные пациента | код fault = `PATIENT_NOT_FOUND`; текст `<message>` — любой |
| 2 | `patient_occurrence_mismatch_code` | Сведения о пациенте отсутствуют или дублируются в документе | Данные пациента | код fault = `PATIENT_OCCURRENCE_MISMATCH`; текст `<message>` — любой |
| 2 | `person_card_not_found_code` | Карточка медработника не найдена в ФРМР | Данные медработника | код fault = `PERSON_CARD_NOT_FOUND`; текст `<message>` — любой |
| 2 | `person_not_found_frmr` | Медработник не найден в ФРМР | Данные медработника | код fault = `PERSON_NOT_FOUND`; текст `<message>` — любой |
| 2 | `pluggable_attrs_occurrence_code` | Дополнительные атрибуты документа не прошли валидацию | Ошибки структуры и валидации | код fault = `PLUGGABLE_ATTRS_OCCURRENCE_MISMATCH`; текст `<message>` — любой |
| 2 | `position_to_role_mismatch_code` | Должность подписанта не соответствует роли подписи | Данные медработника | код fault = `POSITION_TO_ROLE_MISMATCH`; текст `<message>` — любой |
| 2 | `rate_limit_code` | Превышен лимит запросов к РЭМД | Технические ошибки РЭМД | код fault = `RATE_LIMIT`; текст `<message>` — любой |
| 2 | `recipient_mismatch` | Получатель из запроса не найден в СЭМД | Данные пациента | код fault = `RECIPIENT_INFO_MISMATCH`; текст `<message>` — любой |
| 2 | `region_code_different_code` | Адрес пациента не соответствует ФИАС | Данные пациента | код fault = `REGION_CODE_DIFFERENT`; текст `<message>` — любой |
| 2 | `restrict_new_version_code` | Для данного вида ЭМД запрещена регистрация новых версий | Ошибки регистрации в РЭМД | код fault = `RESTRICT_NEW_VERSION`; текст `<message>` — любой |
| 2 | `rmis_region_mismatch_code` | Регион ИС не соответствует региону организации | Ошибки организации / ИС | код fault = `RMIS_REGION_MISMATCH`; текст `<message>` — любой |
| 2 | `rmis_registration_disabled` | ИС зарегистрирована в РЭМД, но не активна: проверьте уведомления и переподключение ИС | Ошибки организации / ИС | код fault = `DISABLED_RMIS`; текст `<message>` — любой |
| 2 | `rmis_registration_missing` | ИС не зарегистрирована в РЭМД или указаны неверные регистрационные данные | Ошибки организации / ИС | код fault = `NO_RMIS`; текст `<message>` — любой |
| 2 | `role_occurrence_mismatch_code` | Подпись роли не соответствует требованиям РЭМД | Ошибки ЭП и сертификатов | код fault = `ROLE_OCCURRENCE_MISMATCH`; текст `<message>` — любой |
| 2 | `series_required_code` | Не указана серия документа, удостоверяющего личность | Данные пациента | код fault = `SERIES_REQUIRED`; текст `<message>` — любой |
| 2 | `signature_decoding_error_code` | Не удалось декодировать электронную подпись | Ошибки ЭП и сертификатов | код fault = `SIGNATURE_DECODING_ERROR`; текст `<message>` — любой |
| 2 | `signature_duplication_code` | Дублирование подписи в документе | Ошибки ЭП и сертификатов | код fault = `SIGNATURE_DUPLICATION`; текст `<message>` — любой |
| 2 | `signature_metadata_certificate_mismatch` | Данные подписи не соответствуют данным документа | Ошибки ЭП и сертификатов | код fault = `VALUE_MISMATCH_METADATA_AND_CERTIFICATE`; текст `<message>` — любой |
| 2 | `signature_verification_error` | Не удалось проверить электронную подпись | Ошибки ЭП и сертификатов | код fault = `SIGNATURE_VERIFICATION_ERROR`; текст `<message>` — любой |
| 2 | `signer_org_mismatch_code` | Организация подписанта не соответствует организации документа | Данные медработника | код fault = `SIGNER_ORG_MISMATCH`; текст `<message>` — любой |
| 2 | `staff_data_frmr` | Данные медработника не соответствуют ФРМР | Данные медработника | код fault = `VALUE_MISMATCH_METADATA_AND_FRMR`; текст `<message>` — любой |
| 2 | `timeout_code` | Таймаут асинхронной обработки на стороне РЭМД | Технические ошибки РЭМД | код fault = `TIMEOUT`; текст `<message>` — любой |
| 2 | `unknown_algorithm_code` | Неподдерживаемый алгоритм электронной подписи | Ошибки ЭП и сертификатов | код fault = `UNKNOWN_ALGORITHM`; текст `<message>` — любой |
| 2 | `unsupported_document_type` | Неподдерживаемый тип СЭМД в РЭМД | Ошибки регистрации в РЭМД | код fault = `UNSUPPORTED_DOCUMENT_TYPE`; текст `<message>` — любой |
| 2 | `valsys_internal_error_code` | Техническая ошибка на стороне РЭМД | Технические ошибки РЭМД | код fault = `VALSYS_INTERNAL_ERROR`; текст `<message>` — любой |
| 2 | `wrong_creation_date_code` | Дата создания документа позже даты регистрации | Ошибки регистрации в РЭМД | код fault = `WRONG_CREATION_DATE`; текст `<message>` — любой |
| 2 | `wrong_message_id_code` | Неверный идентификатор сообщения (messageId) | Ошибки регистрации в РЭМД | код fault = `WRONG_MESSAGE_ID`; текст `<message>` — любой |
| 2 | `wrong_signature_format_code` | Неподдерживаемый формат электронной подписи | Ошибки ЭП и сертификатов | код fault = `WRONG_SIGNATURE_FORMAT`; текст `<message>` — любой |
| 2 | `xds_cda_processing_code` | ИЭМК: ошибка обработки CDA | Ошибки ИЭМК | код fault = `XDS.CDA.PROCESSINGERROR`; текст `<message>` — любой |
| 2 | `xds_cda_validation_code` | ИЭМК: ошибка валидации структуры CDA | Ошибки ИЭМК | код fault = `XDS.CDA.VALIDATIONERROR`; текст `<message>` — любой |
| 2 | `xds_dictionary_validation_code` | ИЭМК: данные не соответствуют справочнику НСИ | Ошибки ИЭМК | код fault = `XDSDICTIONARYVALIDATIONERROR`; текст `<message>` — любой |
| 2 | `xds_document_unique_id_code` | ИЭМК: некорректный идентификатор документа | Ошибки ИЭМК | код fault = `XDSDOCUMENTUNIQUEIDERROR`; текст `<message>` — любой |
| 2 | `xds_duplicate_unique_id_code` | ИЭМК: документ уже зарегистрирован | Ошибки ИЭМК | код fault = `XDSDUPLICATEUNIQUEIDINREGISTRY`; текст `<message>` — любой |
| 2 | `xds_invalid_document_content_code` | ИЭМК: ошибка валидации структуры CDA | Ошибки ИЭМК | код fault = `XDSINVALIDDOCUMENTCONTENT`; текст `<message>` — любой |
| 2 | `xds_missing_document_code` | ИЭМК: состав пакета не согласован (документы/метаданные) | Ошибки ИЭМК | код fault = `XDSMISSINGDOCUMENT`; текст `<message>` — любой |
| 2 | `xds_missing_document_metadata_code` | ИЭМК: состав пакета не согласован (документы/метаданные) | Ошибки ИЭМК | код fault = `XDSMISSINGDOCUMENTMETADATA`; текст `<message>` — любой |
| 2 | `xds_non_identical_hash_code` | ИЭМК: повторная загрузка с изменённым содержимым | Ошибки ИЭМК | код fault = `XDSNONIDENTICALHASH`; текст `<message>` — любой |
| 2 | `xds_non_identical_size_code` | ИЭМК: повторная загрузка с изменённым содержимым | Ошибки ИЭМК | код fault = `XDSNONIDENTICALSIZE`; текст `<message>` — любой |
| 2 | `xds_patient_id_does_not_match_code` | ИЭМК: ошибка метаданных документа | Ошибки ИЭМК | код fault = `XDSPATIENTIDDOESNOTMATCH`; текст `<message>` — любой |
| 2 | `xds_patient_registration_code` | ИЭМК: пациент не определён | Ошибки ИЭМК | код fault = `XDSPATIENTREGISTRATIONERROR`; текст `<message>` — любой |
| 2 | `xds_registry_busy_code` | ИЭМК: сервис временно недоступен | Ошибки ИЭМК | код fault = `XDSREGISTRYBUSY`; текст `<message>` — любой |
| 2 | `xds_registry_deprecated_doc_code` | ИЭМК: замена версии отклонена (документ уже заменён) | Ошибки ИЭМК | код fault = `XDSREGISTRYDEPRECATEDDOCUMENTERROR`; текст `<message>` — любой |
| 2 | `xds_registry_dup_uid_msg_code` | ИЭМК: дублирующийся идентификатор в пакете | Ошибки ИЭМК | код fault = `XDSREGISTRYDUPLICATEUNIQUEIDINMESSAGE`; текст `<message>` — любой |
| 2 | `xds_registry_error_code` | ИЭМК: внутренняя ошибка реестра | Ошибки ИЭМК | код fault = `XDSREGISTRYERROR`; текст `<message>` — любой |
| 2 | `xds_registry_metadata_error_code` | ИЭМК: ошибка метаданных документа | Ошибки ИЭМК | код fault = `XDSREGISTRYMETADATAERROR`; текст `<message>` — любой |
| 2 | `xds_registry_not_available_code` | ИЭМК: сервис временно недоступен | Ошибки ИЭМК | код fault = `XDSREGISTRYNOTAVAILABLE`; текст `<message>` — любой |
| 2 | `xds_registry_out_of_resources_code` | ИЭМК: сервис временно недоступен | Ошибки ИЭМК | код fault = `XDSREGISTRYOUTOFRESOURCES`; текст `<message>` — любой |
| 2 | `xds_replaced_document_org_code` | ИЭМК: замена версии отклонена (другая организация) | Ошибки ИЭМК | код fault = `XDSREPLACEDDOCUMENTORGANIZATIONERROR`; текст `<message>` — любой |
| 2 | `xds_repository_busy_code` | ИЭМК: сервис временно недоступен | Ошибки ИЭМК | код fault = `XDSREPOSITORYBUSY`; текст `<message>` — любой |
| 2 | `xds_repository_dup_uid_msg_code` | ИЭМК: дублирующийся идентификатор в пакете | Ошибки ИЭМК | код fault = `XDSREPOSITORYDUPLICATEUNIQUEIDINMESSAGE`; текст `<message>` — любой |
| 2 | `xds_repository_error_code` | ИЭМК: внутренняя ошибка репозитория | Ошибки ИЭМК | код fault = `XDSREPOSITORYERROR`; текст `<message>` — любой |
| 2 | `xds_repository_metadata_error_code` | ИЭМК: ошибка метаданных документа | Ошибки ИЭМК | код fault = `XDSREPOSITORYMETADATAERROR`; текст `<message>` — любой |
| 2 | `xds_repository_out_of_resources_code` | ИЭМК: сервис временно недоступен | Ошибки ИЭМК | код fault = `XDSREPOSITORYOUTOFRESOURCES`; текст `<message>` — любой |
| 2 | `xds_unknown_patient_id_code` | ИЭМК: пациент не определён | Ошибки ИЭМК | код fault = `XDSUNKNOWNPATIENTID`; текст `<message>` — любой |
| 2 | `xds_unknown_repository_id_code` | ИЭМК: неверный идентификатор репозитория | Ошибки ИЭМК | код fault = `XDSUNKNOWNREPOSITORYID`; текст `<message>` — любой |
| 2 | `xml_validation_error_code` | Ошибка XSD-валидации XML | Ошибки структуры и валидации | код fault = `XML_VALIDATION_ERROR`; текст `<message>` — любой |
| 3 | `certificate_expired` | Сертификат ЭП истёк | Ошибки ЭП и сертификатов | код fault — любой; текст `<message>` ~* `(?is)(сертификат.*истёк|истекш.*сертификат|срок.*действи.*сертификат.*истёк|certificate.*expired)` |
| 3 | `certificate_revoked` | Сертификат ЭП отозван | Ошибки ЭП и сертификатов | код fault — любой; текст `<message>` ~* `(?is)(сертификат.*отозван|certificate.*revoked|revoked.*certificate)` |
| 3 | `cvc_datatype_extended` | Ошибка XSD-валидации XML | Ошибки структуры и валидации | код fault — любой; текст `<message>` ~* `(?is)cvc-datatype-valid|cvc-pattern-valid|cvc-type|cvc-complex-type|cvc-attribute|cvc-elt|cvc-identity-constraint|c...` |
| 3 | `document_creation_date_mismatch_request` | Дата создания документа в ЭМД не совпадает с датой в запросе на регистрацию | Ошибки регистрации в РЭМД | код fault — любой; текст `<message>` ~* `(?is)Дата создания документа в ЭМД \[.*?\] отличается` |
| 3 | `document_kind_not_actual_text` | Вид документа не актуален на дату создания | Ошибки регистрации в РЭМД | код fault — любой; текст `<message>` ~* `(?is)вид документ.*не актуал` |
| 3 | `document_revoked_text` | Документ аннулирован | Ошибки регистрации в РЭМД | код fault — любой; текст `<message>` ~* `(?is)(аннулирован.*документ|документ.*аннулирован)` |
| 3 | `document_uid_mismatch_request` | Идентификатор документа в ЭМД не совпадает с идентификатором в запросе на регистрацию | Ошибки регистрации в РЭМД | код fault — любой; текст `<message>` ~* `(?is)Уникальный идентификатор документа в ЭМД \[.*?\] отличается` |
| 3 | `dul_patient_text` | Документ, удостоверяющий личность пациента: некорректные реквизиты | Данные пациента | код fault — любой; текст `<message>` ~* `(?is)ДУЛ[^А-Яа-я]|реквизит.*удостоверени` |
| 3 | `emd_version_registration_forbidden` | Для данного вида ЭМД запрещена регистрация новых версий | Ошибки регистрации в РЭМД | код fault — любой; текст `<message>` ~* `(?is)запрещена регистрация новых версий` |
| 3 | `object_not_found_text_extra` | Подразделение или запись справочника не найдены на дату документа | Ошибки регистрации в РЭМД | код fault — любой; текст `<message>` ~* `(?is)Подразделение.*(идентификатор|не найден)|подразделение.*не найден` |
| 3 | `org_ogrn_frmo_mismatch` | Несоответствие данных организации в ФРМО | Ошибки организации / ИС | код fault — любой; текст `<message>` ~* `(?is)(ОГРН|ОКПО|КПП|ИНН).*(СЭМД|ФРМО).*(не совпада|не соответств)|ОГРН МО.*не совпада|ФРМО.*(не совпада|не соответс...` |
| 3 | `patient_birth_text` | Дата рождения пациента не заполнена или некорректна | Данные пациента | код fault — любой; текст `<message>` ~* `(?is)Дата рождения пациента` |
| 3 | `patient_fio_mismatch` | ФИО пациента в ЭМД не соответствует данным ЕГИСЗ | Данные пациента | код fault — любой; текст `<message>` ~* `(?is)(Имя|Фамилия|Отчество) пациента в ЭМД \[.*?\] отличается` |
| 3 | `patient_gender_mismatch` | Пол пациента в ЭМД не соответствует данным ЕГИСЗ | Данные пациента | код fault — любой; текст `<message>` ~* `(?is)Пол пациента в ЭМД \[.*?\] отличается` |
| 3 | `provider_org_mismatch_request` | Структурное подразделение (providerOrganization) в СЭМД не совпадает с запросом на регистрацию | Ошибки регистрации в РЭМД | код fault — любой; текст `<message>` ~* `(?is)не совпадает с СП providerOrganization` |
| 3 | `recipient_text_extra` | Получатель из запроса не найден в СЭМД | Данные пациента | код fault — любой; текст `<message>` ~* `(?is)RECIPIENT_INFO_MISMATCH|Получатель.*не найден` |
| 3 | `represented_org_mismatch_request` | Структурное подразделение (representedOrganization) в СЭМД не совпадает с запросом на регистрацию | Ошибки регистрации в РЭМД | код fault — любой; текст `<message>` ~* `(?is)не совпадает с СП representedOrganization` |
| 3 | `schematron_identity_card` | Документ, удостоверяющий личность пациента: некорректные реквизиты | Данные пациента | код fault — любой; текст `<message>` ~* `(?is)IdentityCardType|identity:IssueDate` |
| 3 | `signature_certificate_chain` | Недействительный сертификат подписи | Ошибки ЭП и сертификатов | код fault — любой; текст `<message>` ~* `(?is)(CANT_BUILD_CERT_CHAIN|цепочк.*сертификат|аккредитованн.*УЦ)` |
| 3 | `signature_doc_date_mismatch` | Сертификат подписи недействителен на дату создания документа | Ошибки ЭП и сертификатов | код fault — любой; текст `<message>` ~* `(?is)(DOC_DATE_MISMATCH_CERT_NOT_(BEFORE|AFTER)|сертификат.*не ?действителен.*дат[уы] создания)` |
| 3 | `signature_mo_date_after_request` | Дата подписи МО позже даты поступления запроса на регистрацию | Ошибки регистрации в РЭМД | код fault — любой; текст `<message>` ~* `(?is)Дата и время создания подписи МО \[.*?\] не может быть позже` |
| 3 | `xds_pat_001_text` | ИЭМК: пациент не определён | Ошибки ИЭМК | код fault — любой; текст `<message>` ~* `(?is)\yPAT-001\y` |
| 3 | `xml_parse_error` | Ошибка разбора XML-структуры документа | Ошибки структуры и валидации | код fault — любой; текст `<message>` ~* `(?is)(SAXParseException|org\.xml|ParseError|XML.*parse.*error)` |
| 3 | `xsd_validation` | Ошибка XSD-валидации XML | Ошибки структуры и валидации | код fault — любой; текст `<message>` ~* `(?is)(\ycvc-|XML_VALIDATION_ERROR|\yxsd\y|Invalid content was found)` |
| 4 | `crl_unavailable` | Недоступен сервис проверки статуса сертификата (CRL/OCSP) | Ошибки ЭП и сертификатов | код fault — любой; текст `<message>` ~* `(?is)(\yCRL\y|\yOCSP\y|список.*отозванн)` |
| 4 | `doctor_position_frmr_text` | Должность врача не соответствует данным ФРМР | Данные медработника | код fault — любой; текст `<message>` ~* `(?is)(ФРМР|FRMR).*(должност|specialit|специальност)|(должност|specialit|специальност).*(ФРМР|FRMR)|(должност|specia...` |
| 4 | `document_file_runtime_error` | Не удалось получить файл ЭМД из предоставляющей ИС | Ошибки получения файла ЭМД | код fault — любой; текст `<message>` ~* `(?is)(getDocumentFile|получения файла ЭМД|файлового хранилища)` |
| 4 | `nsi_dictionary_value` | Ошибка справочника НСИ | Ошибки справочника НСИ | код fault — любой; текст `<message>` ~* `(?is)(Справочник OID|codeSystem|codeSystemVersion|верси[яи].*справочник|значени[ея].*НСИ|не соответствует наименова...` |
| 4 | `org_generic_fallback` | Ошибки организации | Ошибки организации / ИС | код fault — любой; текст `<message>` ~* `(?is)(организаци|ОГРН|ФРМО|лицензи)` |
| 4 | `patient_data_gip_text` | Данные пациента не соответствуют ГИП | Данные пациента | код fault — любой; текст `<message>` ~* `(?is)(ГИП|GIP).*(пациент|patient)|(пациент|patient).*(ГИП|GIP)|(данн|сведени).*(пациент|patient).*(не соответств|не...` |
| 4 | `person_frmr` | Данные медработника не соответствуют ФРМР | Данные медработника | код fault — любой; текст `<message>` ~* `(?is)(ФРМР|FRMR).*(не соответств|не совпад|не найден|отсутств)|(не соответств|не совпад|не найден|отсутств).*(ФРМР|...` |
| 4 | `person_snils` | СНИЛС не найден или не соответствует данным пациента/медработника | Данные пациента | код fault — любой; текст `<message>` ~* `(?is)(СНИЛС|SNILS)` |
| 4 | `remd_runtime_internal` | Техническая ошибка на стороне РЭМД | Технические ошибки РЭМД | код fault — любой; текст `<message>` ~* `(?is)(INTERNAL_ERROR|RUNTIME_ERROR|внутренн.*ошиб|непредвиденн.*ошиб|невозможно обработать)` |
| 4 | `snils_invalid_text` | Неверный формат или контрольная сумма СНИЛС | Данные пациента | код fault — любой; текст `<message>` ~* `(?is)(СНИЛС.*неверн|неверн.*СНИЛС|СНИЛС.*контрольн|контрольн.*СНИЛС)` |
| 4 | `transport_network` | Сетевая ошибка | Ошибки связи | код fault — любой; текст `<message>` ~* `(?is)(network|connection|transport|соединени|сетевая ошибка)` |
